# Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# MongoDB Connection String (Atlas or Local Instance with Replica Set for Transactions)
MONGODB_URI=mongodb://localhost:27017/last_mile_delivery?replicaSet=rs0

# JWT Authentication Secret
JWT_SECRET=super_secret_jwt_key_unthinkable_solutions_2026
JWT_EXPIRES_IN=7d

# Google Gemini API Key (For Agentic AI Address Parsing)
GEMINI_API_KEY=your_gemini_api_key_here

# Nodemailer / Email Service Settings
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=ethereal_user@ethereal.email
SMTP_PASS=ethereal_pass

# Twilio / SMS Service Settings (Optional - Mock mode active if empty)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
